{
  "name": "thrift-website",
  "version": "1.0.0",
  "private": true,
  "main": "local-server.js",
  "description": "Full-stack thrift fashion marketplace",
  "scripts": {
    "start": "node local-server.js",
    "vercel-build": "npm run check && mkdir -p public && cp index.html robots.txt sitemap.xml public/ && cp -r css js images public/",
    "check": "node --check local-server.js && node --check js/script.js"
  }
}